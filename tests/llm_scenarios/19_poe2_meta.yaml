name: poe2-meta
category: ninja
goal: >
  I play PoE2, not PoE1. What's the current PoE2 meta? What are the most
  popular classes? Search for top builds.
expected:
  must_succeed: true
  commands_must_include:
    - "poe ninja meta summary"
    - "poe ninja builds search"
  must_not_find:
    - "traceback"
    - "Unknown class"
  blocked: false
